<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.prometheusmetricregistrationcompanion</id>
    <name>Prometheus Metric Registration Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning icon on a <code>Counter.build()....register()</code>
        (also <code>Histogram</code>/<code>Gauge</code>/
        <code>Summary</code>, Prometheus's Java client's four metric
        types) construction written inside a regular method body --
        the client's own official documentation states "the best way
        to register a metric is via a static final class variable".
        Registration happens against the process-global default
        registry: calling <code>.register()</code> more than once for
        the same metric name throws a real
        <code>IllegalArgumentException</code> ("Collector already
        registered that provides name...") at runtime, not just a
        style nit.</p>
        <p><b>100% static text/PSI analysis</b> -- matches by simple
        text, so it works whether the real Prometheus client jar is on
        the classpath or not. Java and Kotlin.</p>
        <p>Stated honestly: v0.1 only flags the "build from scratch
        inside a regular method" shape -- a metric stored as a static
        final field (the documented correct pattern) is never flagged.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.prometheusmetricregistrationcompanion.gutter.MetricRegisteredPerCallLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider language="kotlin"
            implementationClass="dev.gaphunter.prometheusmetricregistrationcompanion.gutter.MetricRegisteredPerCallLineMarkerProvider"/>
        <notificationGroup id="Prometheus Metric Registration Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
